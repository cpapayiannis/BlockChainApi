services:
  blockchain-api:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - blockchain-data:/app/data
    environment:
      - ConnectionStrings__Default=Data Source=/app/data/blockchain.db

volumes:
  blockchain-data:
